config:
  namespace: eyetap

app:
  image:
    repository: ghcr.io/eye-tap/frontend
    tag: latest
    pullPolicy: Always

  name: eyetap-frontend
  port: 8080

  resources:
    requests:
      cpu: null
      memory: null
    limits:
      cpu: null
      memory: null
